# Application
BASE_URL=http://localhost:5000
SECRET_KEY=change-me-to-a-secure-random-string

# Database (Postgres example)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bankops_db
DB_USER=bankops_user
DB_PASS=supersecurepassword

# Test settings
# If you run UI tests with Selenium, point to the webdriver or use headless
CHROME_BIN=/usr/bin/chromium-browser
CHROMEDRIVER_PATH=/usr/bin/chromedriver
SELENIUM_HEADLESS=true

# CI / Test toggles
RUN_UI_TESTS=false   # set to true to run Selenium UI tests in CI/local
